[tox]
envlist = py36
skipsdist = True

[testenv]
commands = {envpython} -Wall tests.py
# TODO: Remove duplication of dependencies between setup.py and tox.ini.
deps =
    lxml>=3.3.3
# Use system-wide lxml if available to avoid building it.
sitepackages=True
